import 'package:flutter/material.dart';

class ViewMenusPage extends StatelessWidget {
  const ViewMenusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('我的食谱 — 待开发'),
    );
  }
}
